export class Admin {
    constructor(
        public name?: string,
        public rol?: string,
        public imageUrl?: string,
        public password?: string,
        // tslint:disable-next-line: variable-name
        public _id?: string
    ) {}
}
